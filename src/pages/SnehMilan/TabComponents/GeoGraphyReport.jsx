import React from 'react';
import Pr from 'prop-types';

export const GeoGraphyReport = ({ records }) => {

    return (
        <h2>This is Geography Report ..</h2>
    );

   
}

GeoGraphyReport.propTypes = {} // add entry for records here